apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: heart-api-ingress
spec:
  rules:
    - host: heart.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: heart-api-svc
                port:
                  number: 80
